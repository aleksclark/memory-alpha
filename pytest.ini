[pytest]
testpaths = tests
markers =
    integration: marks tests that require external services to be running
    asyncio: marks tests that use asyncio
addopts = --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function