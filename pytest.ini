[pytest]
testpaths = tests
markers =
    integration: marks tests that require external services to be running
    asyncio: marks tests that use asyncio
addopts = --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
asyncio_fixture_loop_scope = session
asyncio_check_event_loop_policy = true